---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add vectorized UTC/GMT `ISOdate()` construction and bounded atomic `expand.grid()` data-frame
generation with Cartesian ordering, factor controls, list inputs, and output-dimension metadata.
