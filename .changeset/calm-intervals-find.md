---
"@nativr/nativr": minor
---

Add usage-ranked `base::findInterval()` interval indices with zoo's irregular-Date window call,
bounded binary search, closure controls, missing values, duplicate breakpoints, and numeric
coercion.
