---
"@nativr/nativr": minor
---

Add the usage-ranked GNU R 4.6.0 `colors()`/`colours()` catalog, including the measured scales
default call, `distinct = TRUE`, true aliases, and registered `grDevices::` lookup.
