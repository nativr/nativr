---
"@nativr/base": minor
"@nativr/runtime": minor
"@nativr/nativr": minor
"@nativr/package-tools": patch
---

Run unchanged, digest-pinned `generics 0.1.4` and `withr 3.0.3` source packages as external
compatibility proofs. Add call-rooted replacement, closure formal and environment replacement,
`bquote()` substitution, mixed language/list combination, list-backed environments, dynamic caller
frames, hook registration, closure-like builtin formals, and a session-scoped `graphics::par()`
subset used by generated pure-R state wrappers.
