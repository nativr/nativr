---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add usage-ranked `as.ordered()` coercion with the measured generics character-vector path, ordered
factor identity, unused-level dropping, name preservation, package-defined S3 method forwarding, and
the `letters` constant required by the measured call.
