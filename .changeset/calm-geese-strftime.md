---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add usage-ranked `strftime` formatting for recycled UTC/GMT date-time vectors, deterministic
C-locale tokens, fractional seconds, names, non-finite values, timezone labels, and custom
`as.POSIXlt` dispatch.
