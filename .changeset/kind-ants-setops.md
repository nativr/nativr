---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add stable `intersect()`, `setdiff()`, and `union()` behavior for atomic vectors, factors, lists,
data-frame columns, missing values, NULL identities, common-type matching, and attribute-free result
shapes.
