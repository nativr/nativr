---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add list/data-frame `within()` data-mask mutation and vectorized real/complex `sin()` semantics,
including GNU R-compatible column ordering, NULL handling, metadata, missingness, and domain
warnings.
