---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add generic `mget()`, a first-class `[[` primitive, and GNU R-compatible `mapply()`/`Map()` result
names, enabling unchanged R6 2.6.1 shallow and recursive deep clone paths.
