---
"@nativr/nativr": minor
---

Add browser-owned runtime and working-directory paths with `R.home`, `dir.create`, `dir.exists`,
`list.files`, `list.dirs`, `getwd`, `setwd`, `normalizePath`, `basename`, and `dirname`.
