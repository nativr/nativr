---
"@nativr/nativr": minor
---

Add the usage-ranked `stats::cycle()` generic and regular-series default with `tsp` metadata,
fractional-frequency coordinates, matrix row cycles, and S3 extension seams for package-owned
time-series classes.
