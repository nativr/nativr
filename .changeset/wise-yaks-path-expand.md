---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add the usage-ranked `path.expand()` slice and its earlier high-reach `file.path()` dependency
measured in diffobj, with vectorized browser path composition, strict expansion inputs, attribute
removal, bounded output, and a documented rule that leaves leading tildes unchanged because no host
home directory is exposed.
