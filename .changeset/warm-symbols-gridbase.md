---
"@nativr/base": minor
"@nativr/runtime": minor
"@nativr/nativr": minor
"@nativr/package-tools": minor
---

Match GNU `as.name` and `as.symbol` identity and atomic first-element coercion, preserve compact
arithmetic-operator spacing in deparsed language, and advance unchanged formula.tools 1.7.1 to
source-blind P7. Make the packaged CLI consume the CommonJS XZ decoder through its portable default
interop surface. Advance the unchanged gridBase 0.4-7 holdout to source-blind P7 through reusable
grid viewport transforms, inherited graphical parameters, rectangle grobs, and GNU-compatible
`par(mfrow=)`/`par(mfg=)` layout state. Classify standard package lifecycle hooks outside ordinary
export-documentation coverage, and advance the unchanged `gsubfn` 0.7 source-blind artifact at P4
through browser-owned `BOD` and `CO2`, generic data-frame aggregation, compound formula language,
all standard `matplot` types, conjugate-gradient `optim`, and atomic `rep()` count coercion. Its
ordered first blocker is now the unresolved `chron` Suggests dependency used by `example:list`.
