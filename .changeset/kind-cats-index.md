---
"@nativr/nativr": minor
---

Add named lists, recursive extraction, one- and two-dimensional selection, finite sequences, and
scalar/vector repetition. Add GNU R-style unique partial `$` matching and `[[ exact=]` control. Add
direct `names<-`, `attr<-`, `class<-`, and `dim<-` replacement-function evaluation.
