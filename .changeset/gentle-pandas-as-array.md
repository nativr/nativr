---
"@nativr/nativr": minor
---

Add frequency-ranked `as.array` and `as.array.default` coercion with S3 method dispatch, lazy dots,
one-dimensional vector shaping, name-to-dimname promotion, and existing-array identity.
