---
"@nativr/base": minor
"@nativr/runtime": minor
"@nativr/nativr": minor
---

Add usage-ranked `sink()` and `sink.number()` with persistent output/message diversion, shared
`capture.output()` ordering, bounded browser-memory connections, and pure-R Worker coverage.
