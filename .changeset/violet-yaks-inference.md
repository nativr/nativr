---
"@nativr/base": minor
"@nativr/nativr": minor
---

Extend the usage-ranked linear-model path with weighted QR covariance, `vcov()`, `confint()`, and
`df.residual()`, including singular fits, parameter selection, model-free fit objects, dimnames,
perfect-fit warnings, and S3 dispatch.
