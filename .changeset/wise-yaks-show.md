---
"@nativr/nativr": minor
---

Add a bounded `methods::show()` generic for diffobj's measured style-display example, including
session-registered S4 and old-class method dispatch, inherited lookup, dynamic result visibility,
and browser-safe default output.
