---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add usage-ranked `base::write()` with GNU R column layout, atomic-storage formatting, append and
connection behavior, bounded browser-memory targets, and unchanged pure-R package/Worker evidence.
Align `cat()` final-newline behavior when its separator vector contains a newline.
