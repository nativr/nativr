---
"@nativr/base": minor
"@nativr/nativr": minor
"@nativr/runtime": patch
---

Add usage-ranked `as.raster` and `is.raster` support for ragg capture matrices, grayscale vectors,
RGB(A) arrays, raster identity, custom S3 methods, and the existing browser RGBA command path.
