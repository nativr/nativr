---
"@nativr/runtime": minor
"@nativr/base": minor
"@nativr/nativr": minor
---

Add usage-ranked `graphics::lines()` and exported `lines.default()` with package-owned S3 dispatch,
shared coordinate normalization, all documented plot types, missing-value path breaks, GNU R-shaped
visibility and style rules, and bounded Worker, Canvas, PNG, and display-list output through the
existing segment and point graphics journal.
