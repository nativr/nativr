---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add bounded browser-native `regexpr()`, `gregexpr()`, and `regmatches()` support with character
locations, match-length metadata, zero-width matching, missing values, names, and inverse gaps.
