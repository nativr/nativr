---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add GNU R-compatible `gl()` factor-pattern generation and a bounded atomic-column data-frame
`merge()` subset with explicit/default keys, duplicate and missing-key matching, outer joins,
sorting, suffixes, and Cartesian products.
