---
"@nativr/nativr": minor
---

Add usage-ranked `graphics::barplot()` and `barplot.default()` with S3 dispatch, GNU R-shaped vector
and matrix midpoint geometry, stacked/beside rendering, source-only package reuse, and bounded
Worker browser graphics.
