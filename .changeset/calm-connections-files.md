---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add bounded session-owned `file()` connections with GNU R-style open, close, access, cursor, seek,
summary, and file-existence behavior, and route text I/O, `cat()`, and `capture.output()` through
those connections without exposing the host filesystem.
