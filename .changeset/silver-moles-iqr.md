---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add usage-ranked `IQR()` and extend `quantile()` across GNU R types 1 through 9, including coercion,
missingness, empty and non-finite inputs, and attribute-removal behavior.
