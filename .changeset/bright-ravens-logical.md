---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add frequency-prioritized `any()` and `all()` logical summaries with empty identities, three-valued
missingness, exact `na.rm` handling, eager argument evaluation, and coercion warnings.
