---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add usage-ranked `base::find.package()` over the browser-owned package library registry, including
GNU R-shaped attached-package order, vector and missing-package behavior, explicit library filters,
immutable core and pure-R bundle roots, unchanged package self-discovery, and Worker coverage.
