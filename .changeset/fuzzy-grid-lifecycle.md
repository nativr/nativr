---
"@nativr/nativr": minor
---

Add package-neutral grid `makeContent` and `makeContext` S3 grob lifecycle generics with identity
defaults, registered package-method dispatch, and `NextMethod` class-chain behavior.
