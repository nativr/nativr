---
"@nativr/base": minor
"@nativr/runtime": minor
"@nativr/protocol": minor
"@nativr/nativr": minor
---

Add isolated, configurable session environment variables with GNU R-compatible `Sys.getenv()`,
`Sys.setenv()`, and `Sys.unsetenv()` behavior, including Worker transport, reset restoration, and
unchanged `withr::with_envvar()` execution.
