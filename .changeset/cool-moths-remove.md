---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add frequency-prioritized `rm()`/`remove()` binding deletion with captured names, `list=`, explicit
and inherited environments, warnings, and invisible NULL results.
