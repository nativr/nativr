---
"@nativr/nativr": minor
---

Add browser-safe `print()` and `cat()` output with GNU R-compatible return visibility, collected
`evalDetailed().output`, inline and Worker output callbacks, output-budget enforcement, and
Playground rendering.
