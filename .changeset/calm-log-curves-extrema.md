---
"@nativr/base": minor
"@nativr/nativr": minor
---

Make additive curves inherit the active unexpanded linear or logarithmic window, expose the generic
attribute-stripping `pmin.int` and `pmax.int` Base helpers, bind one-dimensional arrays with their
dimension names, and convert mixed data frames through the standard numeric `data.matrix` path.
