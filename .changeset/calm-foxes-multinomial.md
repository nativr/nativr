---
"@nativr/nativr": minor
---

Add browser-native multinomial matrix semantics, public `mean.default`, Summary `NULL` omission, and
generic optional-example dependency classification, advancing unchanged nor1mix package evidence to
P4 with call-valued symbolic differentiation retained as the next explicit blocker.
