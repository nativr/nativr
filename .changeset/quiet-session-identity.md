---
"@nativr/nativr": minor
---

Add a stable positive `Sys.getpid()` session identity across inline, Worker, reset, and package-code
execution paths without exposing a host operating-system process.
