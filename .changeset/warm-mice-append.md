---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add type-promoting `append()` across atomic, list, factor, pairlist, expression, and matrix shapes,
plus metadata-preserving real/complex `cos()` with missingness and domain-warning behavior.
