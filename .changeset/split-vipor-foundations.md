---
"@nativr/runtime": minor
"@nativr/base": minor
"@nativr/nativr": minor
---

Add generic `split<-` replacement with S3 dispatch across browser-owned vector, matrix, list, and
data-frame values; admit and validate `plot.default(las=)`; expose `stats::ave` from its GNU R
namespace; and decode standard ASCII aliases in bounded version-3 serialized package data.

Use those reusable foundations to advance unchanged vipor 0.4.7 through all applicable package
checks and an independent GNU R-matched scenario spanning all 13 exports to scoped P7, without
package-specific rewriting or relaxed resource limits.
