---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add the usage-ranked `stats::weights()` S3 generic required by measured loo and posterior calls,
including default exact/partial component extraction, lazy dots, `na.exclude` restoration, existing
weighted-lm access, and package-owned method dispatch.
