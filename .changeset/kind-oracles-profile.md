---
"@nativr/nativr": minor
"@nativr/package-tools": minor
---

Target GNU R 4.6.1 explicitly, add named runtime resource profiles and `pmax`, preserve observable
attribute/data-frame construction semantics, and introduce recursive Oracle v2 plus a tiered pinned
pure-R package corpus with an unchanged `labeling` P4 proof and explicit P5 blocker. Expand
recursive proofs across shared environments, language calls, and closures; add parent-environment
replacement and `methods::is`; and record a digest-separated holdout rotation with exact first
blockers.
