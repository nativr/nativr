---
"@nativr/runtime": minor
"@nativr/base": minor
"@nativr/nativr": minor
---

Add bounded `readLines()` and `writeLines()` support for session text and immutable package files,
retain package metadata and R source for virtual lookup, and provide interruptible Worker-safe
`Sys.sleep()` semantics.
