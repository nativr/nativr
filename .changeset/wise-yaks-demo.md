---
"@nativr/nativr": minor
---

Add the browser-owned empty `utils::demo()` catalog shape and explicit errors for external package
demo discovery or execution, which requires package-library and host-resource support.
