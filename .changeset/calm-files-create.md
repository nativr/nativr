---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add usage-ranked `base::file.create()` with GNU R-shaped dots and `showWarnings` matching,
vectorized zero-byte creation and truncation, preflight validation, per-path warnings, pure-R
package execution, virtual-filesystem resource limits, and default Worker coverage.
