---
"@nativr/base": minor
"@nativr/runtime": minor
"@nativr/nativr": minor
---

Add first-class GNU R-shaped `[[<-` callable replacement with trailing positional `value` matching,
recursive list paths, dimensional replacement, deletion and extension, and ordinary S3/S4 dispatch.

Discover, catalog, decode, attach, and execute browser-owned installed package demo scripts through
`utils::demo()` without ambient filesystem or network access. Use these shared contracts to advance
the unchanged registry 0.5-1 source-blind artifact through every applicable package check to scoped
P7 without package-specific rewriting.
