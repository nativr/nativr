---
"@nativr/nativr": minor
---

Add the usage-ranked `stats::density` S3 dispatch boundary used by posterior and distributional,
plus an independently implemented bounded Gaussian `density.default` path.
