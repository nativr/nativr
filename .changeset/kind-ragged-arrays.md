---
"@nativr/runtime": minor
"@nativr/base": minor
"@nativr/nativr": minor
---

Add usage-ranked `base::tapply()` ragged-array grouping with factor-level dimensions, scalar
simplification, list-array results, forwarded arguments, `FUN = NULL` group codes, and bounded
allocation. Repair dimensional extraction from list arrays so `[[` callbacks can consume those
results.
