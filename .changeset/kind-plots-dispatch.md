---
"@nativr/nativr": minor
---

Add an S3-first `plot()` generic and a bounded browser-native numeric `plot.default()` path with
point, line, histogram, step, panel, annotation, Worker graphics, and package-method support.
