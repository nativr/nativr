---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add usage-ranked `utils::glob2rx()` with rprojroot's measured `DESCRIPTION` pattern, vectorized
wildcard conversion, anchored and optionally trimmed expressions, coercion, attribute removal,
logical controls, and bounded browser-only execution.
