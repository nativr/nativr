---
"@nativr/base": patch
"@nativr/nativr": patch
---

Expose the provenance-audited `datasets::iris3` array through the generic browser-owned package-data
path, derived from the independently licensed Iris resource with GNU-compatible values, dimensions,
and dimension names.
