---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add metadata-preserving real `floor()` and factor-aware `split()` grouping across vectors, lists,
matrices, expressions, pairlists, and data-frame rows.
