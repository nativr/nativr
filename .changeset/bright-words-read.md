---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add usage-ranked `base::readChar()` over raw vectors, immutable package resources, mutable session
files, and evaluator-owned file, URL, and gzip connections with UTF-8/byte counting, cursor, EOF,
warning, invalid-input, and resource-limit behavior.
