---
"@nativr/base": minor
"@nativr/nativr": minor
---

Execute standard numeric-response custom GLM-family callbacks through fitting, residuals, summaries,
and response prediction; match `vcov.lm()` method arguments while preserving lazy extra dots; and
advance the unchanged, digest-pinned qvcalc 1.0.4 package to scoped P7 without package-specific
runtime logic.
