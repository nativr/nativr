---
"@nativr/nativr": minor
"@nativr/package-tools": minor
---

Load pure-R package `data/*.R` and delimited text datasets through `utils::data`, and add bounded
browser-memory `read.table`/`read.csv`/`read.delim` plus `write.table`/`write.csv` families.
