---
"@nativr/nativr": minor
---

Add usage-ranked `time()` sampling coordinates for ordinary vectors, matrices, and regular time
series with offsets, integer snapping, `tsp` metadata, `ts` result shape, and S3 method dispatch.
