---
"@nativr/nativr": patch
"@nativr/package-tools": patch
---

Match GNU R S4 slot replacement and atomic-data slot initialization, add lazy `...names()` behavior,
and classify missing `Suggests` or `Enhances` dependencies generically during unchanged pure-R
package checks.
