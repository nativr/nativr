---
"@nativr/base": patch
"@nativr/nativr": patch
---

Expose the locked, zero-argument browser-owned `R_compiled_by()` compatibility binding with the
portable GNU R result shape and explicit NativR toolchain labels.
