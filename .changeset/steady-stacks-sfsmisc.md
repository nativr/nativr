---
"@nativr/base": patch
"@nativr/nativr": patch
---

Add independently licensed browser-owned `stackloss` projections and the complete `airquality` data
frame through the generic `datasets` resource path.
