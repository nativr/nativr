---
"@nativr/base": minor
"@nativr/runtime": minor
"@nativr/nativr": minor
"@nativr/package-tools": minor
---

Add package-neutral S4 parent-object initialization, target-aware `setAs()` coercions, `slot()` and
`slot<-` accessors, and `image.default(asp=)` window handling.

Classify GNU R version/platform-bound `.Rout.save` transcripts as explicitly not applicable while
still executing their retained package tests. Use the reusable semantics and package-check policy to
advance the unchanged, digest-pinned pixmap 0.4-14 source package through its applicable checks and
an independently authored GNU R-matched constructor/coercion/channel/subsetting scenario.
