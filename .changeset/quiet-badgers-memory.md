---
"@nativr/base": minor
"@nativr/runtime": minor
"@nativr/nativr": minor
---

Add usage-ranked `gc()` and adjacent `gcinfo()` over a deterministic census of the reachable NativR
R-value graph, including GNU R-shaped reports, resettable maxima, verbose output, and shared
`system.time(gcFirst)` collection accounting without claiming access to the host JavaScript heap.
