---
"@nativr/nativr": minor
---

Add usage-ranked `outer()` and `%o%` with vector/array dimension metadata, lazy function dispatch,
the measured scales radial-matrix path, and ordinary user-defined infix-call normalization.
