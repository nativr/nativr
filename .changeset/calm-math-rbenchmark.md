---
"@nativr/base": patch
"@nativr/nativr": patch
---

Apply Math group generics column-wise to numeric-alike data frames, dispatch `is.numeric` methods,
and account large colon kernels in bounded chunks while retaining deterministic resource guards.
