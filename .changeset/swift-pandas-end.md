---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add usage-ranked `end()` time-series endpoint semantics with row-shaped defaults, period/cycle
coordinates, configurable tolerance, metadata validation, and custom S3 dispatch.
