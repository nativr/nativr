---
"@nativr/base": minor
"@nativr/runtime": minor
"@nativr/nativr": minor
---

Add GNU-compatible `$` mutation for call objects, model-frame subset evaluation, top-level S4
generic promotion with ordinary-function fallback, `methods::extends` class-lineage queries,
callable model-matrix contrast generators, single-/multiple-response `stats::lm.fit` QR results,
nested list-to-data-frame expansion, model-frame row preservation, terms-driven intercepts, and
S4-aware NA actions.

Advance the unchanged modeltools 0.2-24 artifact through all installed examples, all 46 retained
`regtest.R` expressions, all applicable package checks, and an independent package scenario. Its
current corpus evidence therefore reaches the scoped P7 gate without package-specific rewriting.

Add browser-native central `stats::qchisq()` and `stats::qf()` quantiles with vector recycling,
stable tail/log-probability inversion, attributes, documented boundaries, and explicit non-central
rejection. Use them to load unchanged ellipse 0.5.0 and record `stats::arima0` as its next shared
installed-example blocker.

Add GNU-compatible `utils::findMatches()` and `utils::rc.settings()` behavior plus private,
hierarchy-aware Reference Class `callSuper()` routing. Use the shared contracts to advance unchanged
GlobalOptions 0.1.4 through every applicable package check and an independent scenario to scoped P7.
