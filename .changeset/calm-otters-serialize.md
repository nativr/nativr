---
"@nativr/base": minor
"@nativr/runtime": minor
"@nativr/nativr": minor
"@nativr/package-tools": minor
---

Add bounded GNU R XDR v2/v3 and gzip serialization for `serialize()`/`unserialize()`,
`saveRDS()`/`readRDS()`/`infoRDS()`, and `save()`/`load()`. Load packaged `data/*.rda` and
`R/sysdata.rda` resources into pure-R package environments without translating package code.
