---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add usage-ranked browser-native `kmeans()` clustering with finite vector, matrix, and data-frame
inputs, explicit or deterministic session-random starts, `nstart` selection, four documented
algorithm choices, standard result fields, and differential GNU R evidence.
