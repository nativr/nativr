---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add usage-ranked `base::shQuote()` with browser-default Unix `sh`, explicit `csh`, `cmd`, and `cmd2`
quoting, partial type matching, character coercion and S3 dispatch, pure-R package and Worker
coverage, and GNU R-compatible closure type reporting for R-level builtin wrappers.
