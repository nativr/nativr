---
"@nativr/base": minor
"@nativr/nativr": minor
---

Add `rev()` and the coherent `cumsum()`/`cumprod()`/`cummax()`/`cummin()` family with GNU R types,
attributes, missing propagation, complex arithmetic, and integer-overflow warnings.
