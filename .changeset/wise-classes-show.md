---
"@nativr/nativr": minor
---

Add usage-ranked `methods::showClass()` introspection with package-owned class metadata,
inheritance, slot summaries, virtual/subclass reporting, output capture, and pure-R namespace reuse.
