---
"@nativr/base": patch
"@nativr/nativr": patch
---

Support logical and numeric `format(..., scientific=)` controls, including integer penalties and the
session `scipen` default, through the shared atomic formatting path.
