---
"@nativr/nativr": minor
---

Add usage-ranked `comment()` and `comment<-` support for owned values, including the measured zoo
replacement/query shape, removal semantics, missing character comments, and matching `attr<-`
validation.
