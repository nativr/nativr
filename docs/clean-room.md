# Clean-room policy

NativR is an independent Apache-2.0 implementation. It does not incorporate, translate, link, or
derive implementation code from GNU R, webR, or R packages merely to reproduce their behavior.

Permitted evidence:

- public language and package documentation;
- published mathematical or statistical definitions;
- public APIs and specifications;
- black-box input/output observations from a separately installed R runtime;
- permissively licensed dependencies after license review.

Versioned public catalogs may be recorded as compact observable data when their order, values, and
selection behavior are independently reproduced from documentation and black-box outputs. Such
catalogs must identify provenance and must not be extracted from implementation source.

Prohibited without explicit legal and maintainer review:

- copying or mechanically translating GNU R or GPL package source;
- using webR or GNU R as a shipped implementation dependency;
- copying tests when their license is incompatible or provenance is unclear;
- implying affiliation or using official R branding.

Semantic contributions must identify their behavioral sources, add independently written tests and
conformance cases, and update the compatibility contract. The optional R oracle records canonical
type/value/warning observations only; it never copies implementation source. Reviewers must reject
unexplained large translations or suspiciously source-shaped changes and escalate uncertain
provenance before merging.

Serialization work may use the public R Internals serialization description and black-box byte
outputs from a separately installed R process. Small byte fixtures must record that provenance and
cover only observable interchange behavior. GNU R serializer/deserializer source, headers, tests, or
mechanically translated control flow are not implementation inputs.

Unmodified third-party source packages may be downloaded transiently in opt-in external execution
tests to prove that the package loader works without package-specific rewrites. Those sources are
test inputs under their own licenses: they are not copied into this repository, translated into the
runtime, or treated as implementation evidence. A package artifact used by an application likewise
retains the package's license independently of NativR's Apache-2.0 runtime.

Profile 0.352's symmetric eigensolver is built from the independently licensed Reference-LAPACK
3.12.1 release, not from GNU R, webR, or an R package implementation. LAPACK is permissively
licensed and its complete binary-redistribution notice is retained in the repository and npm-package
`NOTICE` files. The checked-in artifact is deterministic WebAssembly generated by the documented
build script; translated intermediate C stays in a machine-local temporary directory and is not an
implementation input for hand-written runtime logic. GNU R remains a black-box result oracle only.

Profile 0.359's bounded optimizer follows the same independent-source rule. Its generated Wasm is
built from the official BSD-3-Clause L-BFGS-B 2.1 distribution, translated with the independently
distributed Netlib f2c tool, with source, tool, header, and artifact hashes enforced by the build.
Only the generated Wasm module is retained; intermediate translated code stays in a machine-local
temporary directory. The handwritten adapter was derived from the documented reverse-communication
interface and black-box numeric behavior, not GNU R or webR implementation source.

Profile 0.456's core `graphics::arrows` example is independently authored browser demonstration
code. GNU R was queried only for black-box structural observations: the topic creates double `x` and
`y` vectors of length 12, integer `s = 1:10`, and an invisible example result. Neither GNU R Rd
content nor example source was read, copied, translated, or retained.

Profile 0.458's nonlinear least-squares implementation was derived from published numerical
least-squares methods and independently written bounded finite-difference/Gauss-Newton code. GNU R
was queried only through public calls to freeze formals, fitted coefficients, residual summaries,
convergence fields, and profile result shape. No GNU R source file, package data file,
serialization, or mechanically translated control flow was used or copied. One public-object
attribute dump incidentally printed closure text embedded in the fitted object; that text was
ignored and was not an implementation input. The unchanged GPL `sfsmisc` archive remained a
transient external test input only.

Profile 0.459's `stats::lm.influence` demonstration is independently authored from the public
callable contract and already admitted `LifeCycleSavings` facts. GNU R was queried only for
black-box result names, visibility, model side-effect class, rounded coefficients, and residual
length. No GNU R Rd content, example source, package data file, or implementation source was copied.

Profile 0.460's `state` family is a clean-room projection of United States Government geographic,
demographic, regional, and administrative facts. Public Census and USGS publications establish the
underlying observations and classifications. GNU R was queried only through the seven public data
objects to freeze historical ordering, compact map coordinates, labels, types, factor codes,
attributes, and numeric values. No GNU R data file, serialization, package source, Rd content, or
implementation source was read or copied. The unchanged GPL sfsmisc archive remained transient
external test input only.

Profile 0.461's `sunspots` resource uses the NOAA/NCEI UAG-95 publication and archived monthly
listing as independent factual records. The historical compatibility target is the fixed
Andrews-and-Herzberg series documented by GNU R; 143 differing monthly observations are represented
as a declarative compatibility projection over the NOAA baseline. GNU R was queried only through
public-object black-box behavior to freeze those historical values, shape, attributes, and summary
facts. No GNU R data file, serialization, package source, Rd content, implementation source, or
current SILSO Version 2 dataset was read or copied. The unchanged GPL sfsmisc archive remained
transient external test input only.

Profile 0.462's `EuStockMarkets` resource records historical market-index observations described by
the public datasets documentation, which identifies the four indices, fixed dimensions, time span,
and Erste Bank AG as the provider. GNU R was queried only through the public data object as a
black-box catalog to freeze values, ordering, names, classes, and `tsp` metadata. No GNU R package
data file, serialization, source, Rd file, implementation source, or GPL test was copied. A
transient sfsmisc source excerpt incidentally surfaced in diagnostic search output and was ignored;
it was not an implementation input. The checked-in `.rda` bytes were produced by NativR's own
independently implemented serializer from the browser-owned value.

Profile 0.509's `USAccDeaths` resource records historical monthly United States accidental-death
counts identified in published time-series literature. GNU R was queried only through the public
data object as a black-box catalog for factual values, ordering, class, and time metadata. No GNU R
data file, serialization, package source, Rd content, or implementation source was read or copied.

The `arima0` and nonlinear-model extensions were independently implemented from standard Gaussian
time-series likelihood, differencing, covariance, numerical optimization, and nonlinear
least-squares concepts. GNU R public calls supplied only black-box formals, result fields, rounded
numeric observations, and diagnostics. The transient unchanged GPL ellipse archive was test input
only; no package implementation was copied, translated, or embedded.

Profile 0.510's `volcano` resource is a factual projection of the independently maintained
MIT-licensed `mdsumner/volcano` georeferenced TIFF, fixed at 7,356 bytes and SHA-256
`941e9b754afea4c2b6280c8a6fb624cfd8ce284516821a3e70dcd60bf76ed7f3`. The TIFF's 87-by-61
floating-point elevation raster matches the public GNU R object cell-for-cell after the source
repository's documented reversal of both axes. GNU R supplied only black-box type, shape, summary,
and identity observations. No GNU R data file, package source, serialization, Rd content, or
implementation source was read or copied. Perspective title support reuses NativR's existing browser
text journal; unchanged GPL package archives remain transient test inputs only.

Profile 0.511's perspective-facet behavior was derived from public API calls against the pinned GNU
R 4.6.1 black-box oracle and its generated SVG device output. Four distinct facet colours across
multiple azimuths establish source-colour assignment and ascending transformed-depth painter order;
separate outputs establish default and suppressed borders. The unchanged `shape 1.4.6.1` source
archive, SHA-256 `43f9bd0f997fd6cf1838efd8b2509c9a6396513f4e54a20360481634affd22a4`, is only
package-input and package-check evidence. No package function or GNU implementation code is copied,
translated, or embedded in production.

Profile 0.518's `graphics::coplot` slice was independently implemented from public formals,
black-box interval/panel/visibility observations, and NativR's existing browser graphics journal. No
GNU R graphics implementation or package source was read or translated. The newly exposed
`datasets::quakes` blocker remains deliberately unresolved: a GNU R public-object projection may
describe the target but is not an admitted resource without independent redistribution provenance
and a bundle-safe browser-local delivery path.

Profile 0.519 was derived from public R language behavior, frozen package metadata/examples, and
black-box GNU R outputs. The implementation uses NativR's existing value, S3, resource, and RNG
models; it does not inspect or translate GNU R implementation code. The unchanged rbenchmark archive
is executable corpus evidence, not production source or a source of copied algorithms.

Profile 0.520 was derived from public function formals, black-box GNU R values, and the previously
frozen unchanged `invgamma 1.2` archive used only as scheduled corpus input. The exponential
formulas, Poisson-mixture density/inversion, RNG batching, and tests are NativR-owned
implementations. No GNU R source, R package implementation, serialization, or webR code was copied
or translated into production.

Profile 0.521 was derived from public function signatures, public package documentation, and
black-box GNU R 4.6.0 observations used as a non-normative local advisor; GNU R 4.6.1 remains the
target. The Pearson statistic, residual construction, probability tail, condition handling, and
table metadata are independently implemented on NativR-owned primitives. The unchanged GPL
`entropy 1.3.2` archive is used only as an external corpus artifact and black-box execution target;
its implementation source was not read, copied, translated, or linked into NativR.

Profile 0.522's fixed-margin table sampler is an independently written TypeScript implementation of
the published Patefield Algorithm AS 159. Algorithm structure and numeric direction were reviewed
against John Burkardt's independently distributed MIT-licensed MATLAB reference, SHA-256
`7b5a3944ed3c0dbf2d96f9d1847b1bb35a5fd35dc39266d845a5a7acf5374a60`; no GNU R, webR, or R-package
implementation source was inspected or translated. GNU R 4.6.0 supplied only black-box result and
random-state observations as a non-normative local advisor; GNU R 4.6.1 remains the target.
